<?php
    require_once('conexionPG.php');

    $conexionPG = connectPG();
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <title>Rastreo</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="form">
        <div class="form-heading"><h1>Datos para el rastreo</h1></div>
        <form action="connectPG.php" method="POST">
            <label>Vehiculo: </label>
            <input name="vehiculo" class="form-input">
            <label>Tipo de vehiculo: </label>
            <select name="tipo" class="form-select">
                <option value="bus">Bus</option>
                <option value="taxi">Taxi</option>
                <option value="tren">Tren</option>
            </select>
            <label>Usuario: </label>
            <input name="usuario" class="form-input">
            <br>
            <input type="submit" name="submit" value="Ingresar">
            DE AQUI EN ADELANTE ES PARA MOSTRAR UNA TABLA CON TODOS LOS USUARIOS QUE HAY EN LA BASE DE DATOS
            <?php
                $query = "select * from usuarios";
                $resultado = pg_query($conexionPG,$query) or die ("Error en la consulta");
                $nr=pg_num_rows($resultado);
                if($nr>0){
                    echo "<table border=1 bgcolor=skyblue>
                            <tr><td>Usuario</td></tr>";
                    while($filas=pg_fetch_array($resultado)){
                        echo "<tr><td>".$filas["nombre"]."</td></tr>";
                    } echo "</table>";
                }else{
                    echo "No hay datos";
                }
            ?> 
            HASTA AQUI
        </form>
    </div>
    
</body>
</html>


PARA AÑADIR UNA PERSONA A LA BASE DE DATOS
<?php
    $query = "INSERT INTO usuarios (nombre) VALUES ('Roberto')";
    $resultado = pg_query($conexionPG,$query) or die ("Error en la consulta");
?>